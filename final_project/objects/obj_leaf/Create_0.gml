x += random_range(-30,30)
y -= random_range(0,40);
size_decay = random_range(0.002,0.005)
rot_spd = random_range(-10,10)

yspd = random_range(-3,-1)
xspd = random_range(-2,2)

grav_amt = 0.03;

scale = irandom_range(0.7,1);

image_xscale = scale;
image_yscale = scale;

//image_alpha = irandom_range(0.5,0.8);